# Enforce LF line endings for all text files in the repo and on checkout.
# This prevents CRLF from leaking in from Windows editors/tools and
# breaking shell scripts, Dockerfiles, and CI pipelines that run on Linux.
* text=auto eol=lf
