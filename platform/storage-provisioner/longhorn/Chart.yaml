apiVersion: v2
name: longhorn
description: Longhorn distributed block storage provisioner, backed by node-local disk.
type: application
version: 0.1.0
dependencies:
  - name: longhorn
    repository: https://charts.longhorn.io
    version: "1.12.*"
